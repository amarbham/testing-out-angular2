export class User {
    constructor(
        public name: string,
        public address: string,
        public city: string,
        public postcode: string,
        public comment: string,
    ) { }
}